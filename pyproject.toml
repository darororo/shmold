[project]
name = "shmold"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.3.5",
    "ultralytics>=8.3.241",
]

[dependency-groups]
dev = [
    "notebook>=7.5.1",
]
